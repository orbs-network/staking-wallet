version: 2.1
orbs:
  node: circleci/node@4.7.0
jobs:
  build:
    machine: true
    steps:
      - checkout
      - node/install:
          node-version: "14.17.5"
      - run: ./.circleci/build.sh
      - store_artifacts:
          path: /home/circleci/.npm/_logs

workflows:
  build:
    jobs:
      - build
